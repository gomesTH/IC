import { createTheme } from '@material-ui/core';


export const theme = createTheme({
  palette: {
    primary: {
      main: "#999",
    },
  },
  MyButton: {
    backgroundColor: "red",
    color: "white",
    border: "1px solid black",
  },
});